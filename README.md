# AI-помощник нового клиента Faberlic (кейс FB01)

**Команда:** Finik · **Репозиторий:** https://github.com/KaDyK736/FinikTeam-FB01

## 📌 Описание проекта
**Faberlic AI Assistant** — интеллектуальный помощник первичного контакта, который после учебного события регистрации самостоятельно проводит первый диалог с новым клиентом, выясняет его цель и интересы и передаёт наставнику заполненную карточку.  
Главный результат — понятный первый шаг для клиента и сохранённый контекст для наставника.  
Проект выполнен для кейс-чемпионата «ИИ-агенты для бизнеса» (23–25 сентября 2026), кейс FB01 «ИИ помощник нового клиента».

Схема работы: бот принимает сообщение, LLM-скрипт извлекает из свободного ответа структурированные сведения (цель, интересы, подтверждающую цитату), недополученный ответ возвращается на уточнение, итоговая карточка сохраняется в локальной БД и доступна наставнику.

---

## 👥 Команда и роли

| Участник | Роль | Задача |
|---|---|---|
| **Карасев Д. К.** | SA / Product Owner | Требования, эта документация, сценарий демонстрации, приёмка задач команды |
| **Долгова К. О.** | ML Engineer | Скрипт обработки ответа пользователя через LLM (OpenRouter → Mistral AI → локальная Ollama), извлечение структурированных данных в карточку, проверка полноты ответа |
| **Отпущенников А. С.** | DevOps Engineer | Unit-тесты, сборка в Docker, репозиторий и публикация на GitVerse |
| **Веденеев В. С.** | Fullstack | Telegram-бот на aiogram 3, локальная БД, конечная машина состояний (FSM) для вопросов, роль наставника и доступ к карточкам |

> Уточнение по ролям: Долгова консультируется по вопросам БД с Веденеевым. Отклонения от первоначального видения зафиксированы в [Roadmap → Отклонения от видения команды](docs/roadmap.md) — их необходимо сообщать всей команде.

---

## ⚙️ Установка и запуск
### План запуска
1. **Учебные данные кейса** уже лежат в `data/fb01/` (файлы вымышленные, реальные данные не используются). Свой набор можно подложить через `CASE_DATA_DIR` в `.env`.
2. **Запустить LLM-сервис** (один из вариантов, по убыванию предпочтения):
   - Ollama — локальный запуск: `ollama pull qwen2.5:7b-instruct && ollama serve`;
   - OpenRouter — зарегистрироваться, получить ключ, выбрать модель с пометкой **free**;
   - Mistral AI — если OpenRouter недоступен (требуется привязка номера).

   Без модели бот работает: `USE_LLM=0` или недоступный эндпоинт — вопросы, сегмент и
   карточку считает детерминированный движок правил, меню от модели не зависит.
3. **Запустить бота** (Telegram, aiogram 3, SQLite):
   ```bash
   cd bot
   pip install -r requirements.txt
   cp .env.example .env      # вписать TOKEN, ADMIN_TG_ID, MENTOR_PHONE
   python app.py             # токен берётся из .env, в репозиторий не коммитится
   ```
4. **Инициализировать БД**: при первом старте создаётся `bot/fb01_bot.db` и загружаются
   карточки F001–F010 из `data/fb01/registration_events.json`. Пересобрать с нуля —
   `python scripts/rebuild_db.py` (история диалогов при этом удаляется).
5. **Контейнерного контура в репозитории нет**: стенд собран нативно на Windows
   (venv + локальная Ollama), трафик Telegram идёт через `PROXY_URL`. Отклонение от
   плана зафиксировано в [Roadmap](docs/roadmap.md).

### Сценарий проверки первым делом (с защиты)
1. **F002**: первичный диалог → цель «дополнительный доход» → карточка и следующий шаг.
2. **F004**: бот задаёт уточняющий вопрос вместо угадывания цели.
3. **F006**: остановка контакта по просьбе пользователя (`do_not_contact = true`).
4. Вопрос о несуществующей акции → передача обращения наставнику (без выдумывания условий).
5. Повторная загрузка события F001 → карточка **обновляется**, дубликат не создаётся.

---

## 📂 Документация

- [Requirements](docs/requirements.md) — цели, use‑cases, функциональные и нефункциональные требования, метрики производительности, стек технологий.
- [C4 Diagrams](docs/architecture/c4-diagrams.md) — архитектурные диаграммы (Context, Container, Component).
- [Roadmap](docs/roadmap.md) — план разработки по спринтам (кейс-чемпионат, 3 дня), приоритизация MVP, риски и их митигация, отклонения от видения команды.
- [Definitions](docs/definitions.md) — критерии DoR/DoD, метрики успеха продукта и KPI для каждого спринта.

---

## 🛠️ Технологический стек
- **Bot / Backend:** Python 3.11+, aiogram 3 (FSM, middleware), SQLite (локальная БД через `aiosqlite`)
- **ML / LLM-пайплайн:** промпт-извлечение ≥300 строк, последовательность провайдеров OpenRouter (free-модель) → Mistral AI → Ollama (локально), JSON-схема карточки
- **DevOps:** Docker / docker-compose, pytest (unit-тесты), GitVerse (публичный репозиторий)
- **Интеграции:**
  - мессенджер Telegram — демо-интерфейс (по пожеланию держателя — коммуникация через MAX; план подключения MAX описан в Roadmap);
  - учебные данные кейса: `registration_events.json`, `dialogues.json`, `knowledge_base.json`, `segmentation_rules.json`, `tests.json`.

---

## 🚀 MVP
- Загрузка учебного события регистрации с уникальным `client_id` и открытие диалога (без дублей при повторной загрузке).
- FSM-вопросы бота: цель, интересы, доступное время; повторный запрос уже известных сведений не задаётся.
- LLM-скрипт обработки ответа: извлечение цели, интересов и доступного времени в структурированный JSON, возврат «ответ неполный» при отсутствии обязательных полей; пользователь может редактировать свои ответы.
- Сегментация по 4 статусам цели (`personal` / `income` / `business` / `unknown`) только по явному намерению; при двух целях без приоритета — `unknown` и уточнение.
- Карточка клиента: `client_id`, цель, интересы, подтверждающая цитата, следующий шаг, `do_not_contact`, черновик сообщения наставника (`requires_human_approval = true`, автоотправка запрещена).
- Роль наставника: сверка номера телефона с БД; только наставник получает карточку обычного пользователя.
- Остановка диалога по просьбе клиента; вопросы вне базы знаний → передача наставнику.

---

## 📈 Метрики успеха продукта
- Прохождение всех **12 проверок** `tests.json` на учебных данных: 12/12.
- Точность сегментации на 10 содержательных примерах: **100%** (сегмент, отказ от общения, следующий шаг).
- Доля корректно извлечённых структурированных полей из свободных ответов: ≥ 90%.
- Отсутствие запрещённого поведения: 0 обещаний дохода, 0 выдуманных акций, 0 внешних отправок сообщений.
- Среднее время обработки одного сообщения (LLM + БД): ≤ 10 секунд (при локальной Ollama — ≤ 30 с).
- Unit-покрытие ядра (извлечение, валидация, дедупликация): ≥ 70%, все тесты зелёные в CI-прогоне `docker compose up` + `pytest`.

---

## 1. Какую проблему мы решаем?
Первичный контакт с новым клиентом выполняется наставником вручную; цель и интересы человека при этом часто теряются, а время на обработку одного обращения не измерено. Наш помощник автоматизирует один начальный этап: задаёт уточняющие вопросы, сам структурирует ответы и формирует готовую карточку с обоснованием (цитата) и следующим шагом. Наставник получает не переписку, а заполненный контекст и черновик сообщения — и тратит время на человека, а на разбор сообщений.

Важно: автоматизируется **первый диалог**, а не вся воронка продаж. Все сообщения наставника остаются черновиками с ручным подтверждением.

---

## 2. Сегменты пользователей

### Новый клиент (учебные события F001–F010)
Человек, который только что зарегистрировался (в учебном стенде — событие из JSON) и ещё не общался с наставником. По явному намерению из диалога он попадает в один из четырёх учебных сегментов:

1. **Покупка для себя (`personal`)** — ищет товары для личного потребления, интересуется категориями («уход за домом»). Ценит подбор материалов без давления.
2. **Дополнительный доход (`income`)** — ищет подработку, готов уделять ограниченное время (например, «два вечера в неделю»). Чувствителен к обещаниям заработка — гарантировать их нельзя.
3. **Развитие бизнеса (`business`)** — хочет развивать свою группу, имеет опыт руководства. Ему нужна встреча с наставником и конкретные вопросы по формату.
4. **Цель не определена (`unknown`)** — «просто смотрю», нескольких целей без приоритета, или прямое «не пишите мне» (отказ от общения имеет приоритет над всеми маршрутами).

### Наставник
Владелец процесса (структура держателя кейса). Получает заполненную карточку клиента, подтверждает итоговый шаг и отвечает на вопросы вне базы знаний. Определяется по номеру телефона в БД; доступ к карточкам — только у наставников.

---

## 3. Pain Points

### Новый клиент
Ответы приходят в свободной форме, и вручную их не разложить по полям без потерь. На незаданный вопрос о цене или скидке легко получить выдуманный ответ, а обещание дохода или лечебных свойств подрывает доверие. Если цель не определена, клиенту предлагают случайный маршрут — и он отваливается на первом шаге.

**Как решают сейчас:** переписка в мессенджерах вручную, заметки в блокноте/Excel, потеря контекста между сообщениями.

### Наставник
Тратит рабочее время на первичный опрос вместо живого общения; не видит, что человек уже ответил; не знает, задан ли вопрос, на который у него нет ответа в базе. Повторная регистрация клиента создаёт дубли.

**Как решают сейчас:** ручное чтение переписки, карточки заводятся «в голове», история не сохраняется.

---

## 3.1. Анализ текущих решений

**1. Ручной первичный контакт (мессенджер/телефон)**
- Плюсы: живой тон, гибкие вопросы.
- Минусы: цель и интересы теряются; нет единого формата карточки; высокая стоимость времени наставника; нет обработки 10+ обращений параллельно.

**2. Статичная онлайн-анкета (форма на сайте)**
- Плюсы: структурированный ввод сразу по полям.
- Минусы: «всё или ничего» — непонятные ответы не уточняются; низкая доходимость до конца; не умеет работать со свободным текстом.

**3. Готовые чат-боты «по шаблону» (деревья ответов)**
- Плюсы: предсказуемость, дешёвый запуск.
- Минусы: понимают только заранее заданные фразы; нестандартный свободный ответ ломает сценарий; не извлекают цель и интересы из свободной речи.

**4. LLM-ассистент «в лоб» (без БД и правил)**
- Плюсы: свободно ведёт диалог.
- Минусы: выдумывает акции и обещания дохода; ничего не сохраняет; без контроля сегментации и отказа от общения — неприемлемо для бизнеса.

### Возможность для нас: гибрид — FSM-бот с детерминированными учебными правилами сегментации поверх LLM-извлечения свободных ответов, локальной БД и карточки-черновика с обязательным подтверждением наставником. Так закрывается и качество извлечения, и безопасность ответов, и сохранение контекста.

---

## 4. Use‑cases

> Терминология акторов: если в сценарии указано два актора, второй актор (наставник) может выполнять ровно то же самое, что и основной актор (пользователь), и дополнительно — то, что отмечено отдельно.

## UC1 — Ответить на вопрос

| Поле              | Описание |
|-------------------|----------|
| **Название**      | Ответить на вопрос |
| **Акторы**        | Пользователь, Наставник (наставник может делать то же, что пользователь) |
| **Предусловия**   | У актора есть уникальный `client_id` и доступ к боту; карточка существует или создаётся по первому входу |
| **Основной сценарий** | 1. Пользователь зашёл в бота.<br>2. Система задаёт ему вопрос через FSM (цель, интересы, доступное время).<br>3. Пользователь отвечает. |
| **Постусловия**   | Карточка с ответом сохранена в БД; карточка актуализирована (добавлены `client_id`, ответ, подтверждающая цитата, следующий шаг) |
| **Альтернативные сценарии** | Пользователь хочет изменить ранее данный ответ → бот принимает правку, фиксирует новое намерение и сохраняет историю.<br>Ответ неполный → бот переспрашивает именно недостающее поле.<br>Две цели без приоритета → `unknown` + уточняющий вопрос, а не случайный выбор. |
| **Ошибки**        | E1. LLM-сервис недоступен → перебор провайдеров (OpenRouter → Mistral → Ollama), при полном отказе — сообщение об ошибке и сохранение сырого ответа в карточку.<br>E2. Пустой ввод → вежливая просьба переформулировать.<br>E3. Пользователь просит прекратить общение → `do_not_contact = true`, опрос останавливается (KB06). |

---

## UC2 — Создать новую карточку

| Поле              | Описание |
|-------------------|----------|
| **Название**      | Создать новую карточку |
| **Включает**      | UC1 |
| **Акторы**        | Пользователь, Наставник (наставник может делать то же, что пользователь) |
| **Предусловия**   | В системе есть карточка пользователя, но требуется новая; у пользователя есть уникальный `client_id` |
| **Основной сценарий** | 1. В системе уже есть карточка, но пользователю нужна новая.<br>2. Система создаёт новую карточку и открывает первичный диалог.<br>3. Дальше выполняется UC1 (пользователь отвечает на вопросы). |
| **Постусловия**   | Создана новая карточка с заполненными полями; при повторной загрузке события того же `client_id` карточка обновляется, дубликат не создаётся |
| **Альтернативные сценарии** | Карточка с этим `client_id` отсутствует → создаётся первая карточка.<br>Пользователь уже отвечал на вопрос по этой цели → новая правка не создаёт новую карточку, а обновляет текущую. |
| **Ошибки**        | E1. Пустой объект без `client_id` → сообщить о некорректном входе, не смешивать с другой карточкой (FB01‑F011) |

---

## UC3 — Посмотреть карточку

| Поле              | Описание |
|-------------------|----------|
| **Название**      | Посмотреть карточку |
| **Акторы**        | Наставник |
| **Предусловия**   | Наставник написал боту; его номер телефона есть в БД (заранее заполненная заглушка вместо интеграции с сайтом) |
| **Основной сценарий** | 1. Наставнику нужно посмотреть карточку пользователя.<br>2. Бот сверяет номер телефона входящего с таблицей наставников.<br>3. Бот показывает карточку: `client_id`, цель, интересы, цитата, следующий шаг, отказ от общения, история правок. |
| **Постусловия**   | Наставник получил заполненную карточку с историей и открытыми вопросами |
| **Альтернативные сценарии** | Наставник сам отвечает на вопросы бота и редактирует свои ответы → выполняются те же UC1/UC2, что и для пользователя.<br>Все сообщения пользователю от имени наставника — только черновики (`requires_human_approval = true`, автоотправка запрещена). |
| **Ошибки**        | E1. Номер не найден в таблице наставников → интерфейс обычного пользователя без доступа к карточкам.<br>E2. Нет карточек → сообщение «данных пока нет». |

---

## 5. Value Proposition

«Клиент ответил свободным текстом — наставник получил готовую карточку с доказательством и следующим шагом.» Помощник проводит первый диалог по конечной машине состояний, LLM извлекает из ответов структуру (цель, интересы, числа, категории), правила сегментации не дают выдумать цель, а любой спорный вопрос уходит наставнику в виде черновика, который человек подтверждает сам. Так сохраняется каждый ответ клиента и экономится время наставника без рисков для доверия.

---

## 6. North Star Metric

Доля первичных диалогов, завершённых заполненной карточкой с явной целью (или явным отказом), которую наставник принял без доопроса клиента.

---

## 7. Что уже запускается в коде сегодня

Раздел фиксирует фактическое состояние репозитория, чтобы запуск и проверка не
расходились с описанием выше.

Работает и проверяется: загрузка учебного события регистрации, первичный диалог по FSM,
извлечение цели, интересов и красных флагов движком правил с проверенной надстройкой
из модели, одна карточка и одна история диалога на `client_id` в SQLite, сверка номера
телефона при входе, правка анкеты, ответы по базе знаний с передачей остального
наставнику, остановка контакта по просьбе (`do_not_contact`), режим наставника и
доступ к карточкам по номеру телефона.

```
bot/                      рабочий контур: Telegram-бот, движок правил, база, проверки
├── app.py                точка входа: диспетчер, роутеры, старт БД и поллинг
├── assistant/            engine.py — детерминированные правила и карточка;
│                         llm.py — клиент OpenAI-совместимого API; analyzer.py —
│                         скрещивание разбора модели с правилами; answers.py —
│                         ответы по базе знаний; knowledge.py — чтение данных кейса
├── common/               menus.py — шаги диалога; edit_fields.py — поля правки анкеты
├── database/             models.py, repository.py — схема и ORM; case_seed.py —
│                         загрузка карточек кейса; site_export.py — форма регистрации
├── handlers/             client_private.py — диалог клиента; mentor.py — карточки
│                         наставнику; admin.py — /load, /event, /stats стенда
├── kbds/                 reply.py и inline.py — клавиатуры (кнопки не зависят от модели)
├── middlewares/          db.py — сессия БД; user.py — роль по номеру телефона
├── states/               dialogue.py — FSM: шаги диалога, правка анкеты, вопрос
├── utils/                cards.py — тексты карточек; validators.py — телефон и маска
├── scripts/              прогоны проверок и консольный интерфейс (см. «Как проверить»)
├── requirements.txt      зависимости контура
└── .env.example          образец настроек; реальный .env в архив не входит

data/fb01/                учебные данные кейса: события, диалоги, база знаний, tests.json
docs/                     требования, C4, roadmap, определения, план защиты
presentation/             usecase.drawio, usecase.png, презентация команды
legacy/fb01-prototype/    первая версия прототипа: один системный промпт и LLM-извлечение
                          без FSM и базы. Оставлена для истории, не запускается и к сдаче
                          не относится — вся логика переписана в bot/
```

Итог прогонов лежит в `bot/results/`: `FB01_проверки.md` — таблица «id проверки /
факт / время», `_baseline_qwen25_7b_t03.txt` — лог базового замера модели.

Запуск: `pip install -r requirements.txt`, `.env` из `.env.example`, `python app.py`
из папки `bot/` (локально нужен Ollama с `qwen2.5:7b-instruct`).

### Как сошлись правила и модель

Вопросы клиенту задаёт машина состояний, а решение о карточке принимает детерминированный
движок `bot/assistant/engine.py`: он определяет цель, сегмент, следующий шаг и границы
прототипа (KB01–KB08). Модель добавлена поверх и только дополняет разбор
(`bot/assistant/analyzer.py`): она обязана привести дословную цитату из реплики, вывод
сверяется по словарям движка, а цели и приоритет нескольких целей считает по-прежнему
движок — перефразировки и «несколько целей без приоритета» остаются `unknown` (KB04, KB07).
Если `USE_LLM=0`, эндпоинт недоступен или модель вернула битый JSON, работает один движок,
и человек разницы не замечает.

Отдельный слой — ответы на вопросы вне анкеты (`bot/assistant/answers.py`): правило
выбирает движок, формулировку даёт модель, но ответ с цифрой или обещанием отклоняется и
остаётся заготовкой из базы знаний. Чего в базе нет — бот говорит, что не знает, пишет
вопрос в карточку и предлагает пункт меню; наставник отвечает сам.

`missing_information` и черновик сообщения наставнику считаются из карточки, автоматической
отправки нет ни у одного сообщения.

### Переменные окружения

Единственная схема имён — та, что читает код; образец в `bot/.env.example`.

| Переменная | Зачем |
|---|---|
| `TOKEN` | токен бота от @BotFather |
| `PROXY_URL` | прокси до Telegram (на стенде без прямого выхода в сеть) |
| `DB_URL`, `DB_ECHO` | строка подключения SQLite и лог SQL |
| `MENTOR_PHONE` | номера наставников — они дают доступ к карточкам |
| `DEMO_PHONE` | настоящий номер одного клиента, ложится на карточку F001 |
| `ADMIN_TG_ID` | telegram-id, которому доступны `/load`, `/event`, `/stats` |
| `REGISTER_URL` | ссылка регистрации с номером пригласившего |
| `CASE_DATA_DIR` | папка с учебными данными, если она не `../data/fb01` |
| `USE_LLM` | `1` — подключать модель к разбору реплик и формулировке ответов |
| `LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL` | эндпоинт и модель (локально — Ollama) |
| `LLM_TEMPERATURE`, `LLM_MAX_TOKENS`, `LLM_TIMEOUT` | режим генерации и границы ожидания |

### Как проверить

Прогоны запускаются из папки `bot/`, каждый со своей SQLite-базой — рабочая
`fb01_bot.db` не меняется. Учебные входы берутся только из `data/fb01/tests.json`.

```bash
python scripts/run_tests.py           # проверки кейса: движок, загрузчик, доступы, меню
python scripts/bot_flow_test.py       # живой диалог через диспетчер, без токена Telegram
python scripts/check_llm_merge.py     # карточки «правила» против «правила + модель»
python scripts/check_kb_answers.py    # ответы по базе знаний и выход за её пределы
python scripts/check_kb_handlers.py   # то же в настоящих хендлерах
python scripts/check_kb_refusal.py    # отказ останавливает связь по-настоящему (KB06)
python scripts/check_dialog_button.py # «Начать диалог»: один шаг за нажатие
python scripts/console_chat.py        # тот же диалог в консоли
python scripts/rebuild_db.py          # пересобрать базу из файлов кейса
```

Итог последнего прогона на `qwen2.5:7b-instruct`: 174/174 и 64/64, карточки F001–F010
с моделью совпадают с чистыми правилами (10/10), расхождений нет. Таблица «id проверки /
факт / время» — в `bot/results/FB01_проверки.md`, лог базового замера модели — рядом.
Эти прогоны проверяют агента и загрузчик; накопление карточки через FSM, остановку
контакта и доступ наставника проверяют `bot_flow_test.py` и живая проверка в Telegram
(F011, F012 — `/event` и `/load` в админке стенда).
